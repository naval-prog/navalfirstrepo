[project]
name = "agentic_terminal"
version = "0.1.0"
description = "an mcp which adds  terminal capbiltiesto an agent "
readme = "README.md"
requires-python = ">=3.13"
dependencies = [
    "fastmcp>=3.3.1",
    "langchain>=1.3.2",
    "langchain-mcp-adapters>=0.2.2",
]

[tool.uv.workspace]
members = [
    "Mcp-pypi",
]
